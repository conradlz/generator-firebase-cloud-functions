{
  "scripts": {
    "test": "cd functions; ./node_modules/mocha/bin/mocha; cd -",
    "deploy": "cd functions/src; babel ./*.js --presets latest -d ../dist; cd ../dist; uglifyjs-folder ./ -e -o ./ -x .js; cd ../..; firebase deploy --only functions"
  }
}
